#include "simulator.h"

int main() {
  //std::string filename = "../testcases/expr.data";
  //Simulator simulator(filename);
  Simulator simulator = Simulator();
  simulator.run();
  return 0;
}